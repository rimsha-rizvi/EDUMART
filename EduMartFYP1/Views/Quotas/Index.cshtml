@model IEnumerable<EduMartFYP1.Quota>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuotaName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuotaName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuotaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuotaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuotaID })
        </td>
    </tr>
}

</table>
