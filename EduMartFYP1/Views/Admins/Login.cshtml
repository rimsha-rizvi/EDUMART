@model EduMartFYP1.Models.Adminmembership

@{
    ViewBag.Title = "Login";
}
<!-- login -->
<div class="login-w3ls py-5">
    <div class="container py-xl-5 py-lg-3">
        <h3 class="title text-capitalize font-weight-light text-dark text-center mb-5">
            Login
            <span class="font-weight-bold">now</span>
        </h3>
        <!-- content -->
        <div class="sub-main-w3 pt-md-4">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-style-agile form-group">
                    <label>
                        Email
                        <i class="fas fa-envelope-open"></i>
                    </label>
                    @Html.EditorFor(model => model.adminusername, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.adminusername, "", new { @class = "text-danger" })
                </div>
                <div class="form-style-agile form-group">
                    <label>
                        Password
                        <i class="fas fa-unlock-alt"></i>
                    </label>
                    @Html.PasswordFor(model => model.adminpassword, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.adminpassword, "", new { @class = "text-danger" })
                </div>
                <input type="submit" value="Log In">
                <p class="text-center dont-do mt-4 text-white">
                    Don't have an account?
                    @Html.ActionLink("Register Now", "Students/Create", new object { }, new { @class = "text-white  font-weight-bold" })
                </p>
            }
        </div>
        <!-- //content -->
    </div>
</div>
<!-- //login -->
