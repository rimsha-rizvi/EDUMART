@model IEnumerable<EduMartFYP1.Decipline>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeciplineName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeciplineName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeciplineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeciplineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeciplineID })
        </td>
    </tr>
}

</table>
